name: web-linter
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go 1.19
        uses: actions/setup-go@v1
        with:
          go-version: 1.19
        id: go

      - name: Check out code into the Go module directory
        uses: actions/checkout@v1

      - name: gofmt
        run: cd web5 && test -z $(gofmt -l . | tee /dev/stderr)

      - name: linter install
        run: cd web5 && curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s v1.49.0

      - name: linter run
        run: cd web5 && bin/golangci-lint --version && bin/golangci-lint run
